# PTM 项目：上下文恢复钩子

## 强制上下文重建

每当代理开始一个新的上下文、从摘要/压缩上下文恢复，或对当前项目的既有工作状态缺少完整记忆时，必须在回答用户、提出计划、运行命令或修改文件之前，完整读取以下四个文件：

1. `.clinerules/1_PTM项目_Charter_决策与原则.md`
2. `.clinerules/2_PTM项目_Phase0_Runbook.md`
3. `.clinerules/3_PTM项目_导师会议Agenda.md`
4. `whatwedo.md`

读取后，以 Charter 为项目边界和决策原则、以 Runbook 为实验操作依据、以 Agenda 为待讨论决策清单、以 `whatwedo.md` 为最新进度记录。若这些文件与旧摘要或记忆有冲突，以文件内容为准，并在继续工作前向用户简要说明发现的冲突。

不得因为上下文压缩而重复已经完成的实验、覆盖既有输出，或把尚未完成的计划误报为已完成。

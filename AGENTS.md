# PTM 项目：上下文恢复钩子

## 强制上下文重建

每当代理开始一个新的上下文、从摘要/压缩上下文恢复，或对当前项目的既有工作状态缺少完整记忆时，必须在回答用户、提出计划、运行命令或修改文件之前，完整读取以下项目文件与本地日志：

1. `.clinerules/1_PTM项目_Charter_决策与原则.md`
2. `.clinerules/2_PTM项目_Phase0_Runbook.md`
3. `.clinerules/3_PTM项目_导师会议Agenda.md`
4. `whatwedo.md`
5. `.log/plan.log`
6. `.log/terminal.log`
7. `.log/git.log`

读取后，以 Charter 为项目边界和决策原则、以 Runbook 为实验操作依据、以 Agenda 为待讨论决策清单、以 `whatwedo.md` 和 `.log/` 为最新进度记录。若这些文件与旧摘要或记忆有冲突，以文件内容为准，并在继续工作前向用户简要说明发现的冲突。

不得因为上下文压缩而重复已经完成的实验、覆盖既有输出，或把尚未完成的计划误报为已完成。

## Git 原子提交规则

用户要求所有项目更改采用原子提交。每完成一次独立、可说明的文件修改后，必须立即：

1. 检查该文件的 diff；
2. 只暂存本次修改涉及的明确文件，不使用宽泛暂存；
3. 用准确说明本次变化的提交信息创建 Git commit；
4. 确认工作区中没有因本次操作产生的意外未提交文件。

生成的实验产物若属于正式、可复现结果，应按同一轮实验作为一个原子提交；临时测速、诊断或不完整产物不得混入正式结果提交。`whatwedo.md` 按 `.gitignore` 保持本地日志，除非用户明确要求改变这一规则，否则不强制加入 Git。

## 本地行动日志规则

`.log/` 是被 Git 忽略的追加式本地审计目录。每个行动周期都必须更新三个文件；只追加新行，不改写或删除历史记录：

1. `.log/plan.log`：记录当前的新计划、计划调整，或明确写明计划未变。
2. `.log/terminal.log`：记录执行过的终端命令或工具行动及其关键结果；不得写入密钥、令牌或其他敏感内容。
3. `.log/git.log`：记录暂存、提交、生成产物或无 Git 变更；提交时必须包含 commit hash 和变更摘要。

在完成一次行动、向用户报告结果前，先将对应计划、终端和 Git 状态追加到上述日志。上下文恢复后，先读取三个日志，再决定是否继续、重试或清理任何未完成动作。

## 远端算力与外部资源规则

本项目的当前研究范围是构建并验证“encoder 是否必要”的证据包：先选择有判别余量的困难任务，再在同一任务上比较磷酸化单模态、多类 PTM、传统/PCA 基线与标签冻结的 encoder；不在本轮扩展到完整的临床解释或外部临床验证。

允许使用公开 qPTM 数据、公开论文，以及公开的 COMPASS-PTM/PTM-Mamba 代码或论文作为预训练和基线研究资源；使用前必须记录来源、许可、数据用途与其不接触下游监督标签的边界。不得把 COMPASS 的预测结果作为 encoder 输入。

远端算力机只用于执行已在本地完成并原子提交的代码。同步路径固定为“本地 `D:\\coding\\PTM` → GitHub `vacuoleC/PTM` → 远端 `/data/PTM`”；远端只能在 `/data/` 下读写项目，不得在其他路径建立项目、环境、缓存或临时目录。远端一律使用既有 Conda 环境 `ptm-encoder`，不得创建、删除、更新或探测其他环境，也不得做破坏性系统操作。远端结果先写入 `/data/PTM/outputs`，再通过 GitHub 同步回本地；运行前后均记录所用 commit、命令、输出与状态到本地 `.log/`。

若远端缺少 Git 且用户未授权安装或修改环境，允许唯一的传输回退方案：只通过既有 SSH 连接在 `/data/PTM` 内传输**已经在本地原子提交且已推送 GitHub 的明确文件列表**。先上传到同目录临时文件，验证全部临时文件存在后再逐个原子替换；回传正式 `outputs` 也先下载为本地临时文件、核对后再放置并创建本地原子结果提交。不得以此方案同步未提交文件、批量覆盖目录、下载归档包或访问 `/data/` 之外路径。

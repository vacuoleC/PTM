# PTMv2 工作记录

## 当前阶段

PTMv2 刚初始化，目标是把 v1 的“encoder 未显示稳定增益”结论转化为一个可复用的小样本 PTM 可建模性审计平台。v1 目录仅作本地只读参考，已被 Git 忽略。

## 已有文档与结论

- `whathappentov1.md`：记录 v1 的证据、局限，以及 v2 的研究边界。
- `list.md`：已改写为可执行行动指南，规定先冻结研究设计，再依次完成可建模性审计、低容量主比较、失败边界与条件性多视图分析。
- 已建立 `config/project.yaml` 和 `data/manifest.tsv`；`study_design.yaml`、分析代码和正式实验结果尚未创建。

## 下一步

进入 E0.3：冻结 `study_design.yaml`。在主标签、主比较、拆分规则和统计推断被冻结并提交前，不运行模型比较。

## 自动化与记录规则

`AGENTS.md` 已要求上下文恢复时读取本文件、行动指南与 `.log/` 三个追加式日志。本文件是版本化研究账本：每一个原子研究单元必须和对应代码、配置、表格或图像一起提交；`.log/` 仍是本地追加式原始证据。

## 事件树大纲

```text
E0  v2 治理、数据溯源与研究设计
├─ E0.1 目录、配置与数据 manifest
├─ E0.2 版本化工作记录、日志关联与最终对比报告机制
├─ E0.3 主任务、终点与推断规则冻结
└─ E0.4 全局自动化监督器绑定与长间隔心跳
   └─ E0.4.a 5 分钟连续性测试（E2.1 → E2.2）
E1  数据与标签可行性审计
├─ E1.1 病人独立性、标签完整性与类别审计
└─ E1.2 测量缺失与候选特征审计
E2  无泄漏低容量管线
├─ E2.1 训练折内预处理与单元测试
└─ E2.2 嵌套患者级拆分与折外预测记录
E3  可建模性审计
├─ E3.1 固定主管线的置换 null
├─ E3.2 学习曲线与稳定性
└─ E3.3 失败/异常分支（按发现追加）
E4  同级模型比较
├─ E4.1 原始特征 + Elastic Net（主）
├─ E4.2 PCA + Elastic Net（主）
├─ E4.3 TruncatedSVD + 校准线性 SVM（探索）
└─ E4.4 训练折内 UMAP + Elastic Net（探索）
E5  失败边界、数据路线图与 encoder 重新开门判定
E6  条件性多视图分支（仅真实患者级配对且 E3 通过时开启）
E7  最终可复现交付与执行对比报告
```

新增任务必须有父事件、同级比较组（如适用）和“为什么新增”的三问记录；失败、暂停和否定结果保留在树中。

## 事件记录

### E3.1 — 冻结主管线的完整置换 null

- 父事件：E3；状态：进行中
- 为什么做：固定参数 E2.2 冒烟结果不能提供预注册的经验 p 值；主推断要求每次置换均重新执行训练折内处理、内层参数选择及外层预测。
- 怎么做：先实现可测试的嵌套主管线和小规模置换烟雾测试；只有脚本、配置、进度日志和远程启动方式通过校验后，才在 `/data/PTM` 的既有 `ptm-encoder` 环境执行冻结的 500 次完整置换。
- 结果怎么样：已进入实现阶段，尚未运行置换或形成 p 值。
- 证据与决策：`study_design.yaml` 的 `permutation_test` 定义、日志 E3.1。E2.2 固定参数 AUPRC 仅作执行证据，不能代替本事件。

- 实现检查点：新增 `nested_raw_elasticnet.py`，先把原始特征主管线的内层选择和外层 OOF 逻辑独立为可测试核心；待单元测试后再接入真实数据和标签置换。
- 运行器检查点：新增 `run_raw_nested.py`，从冻结设计读取 27 个预注册候选组合并输出观察到的嵌套 OOF 与各外层折选择参数；仅完成编译检查，尚未运行真实高维评估。

### E2.2 — 患者级重复拆分与折外记录基础

- 父事件：E2；状态：已完成
- 为什么做：训练/测试病人必须严格不重叠。
- 怎么做：实现 5 折×10 次重复分层拆分，并逐折断言病人集合无交集。
- 结果怎么样：合成 20 病人测试生成 10 个拆分，1/1 测试通过；提交 `b611d62`。真实标签拆分索引和 OOF 持久化已完成；远程固定参数冒烟在 50 个外层折（10 次重复）上生成 1,060 条验证通过的 OOF 概率，10 次重复 AUPRC 为 0.4599 ± 0.0262，随机基线为 0.4528。该固定参数结果只证明端到端管线可运行，未执行内层选择、置换或主模型推断。
- 证据与决策：`scr/splits.py`、`tests/test_splits.py`、`outputs/tables/e2_2_fixed_parameter_oof_smoke.csv`、`outputs/tables/e2_2_fixed_parameter_oof_smoke_fold_summary.csv`、`outputs/tables/e2_2_fixed_parameter_oof_smoke_repeat_summary.csv`、日志 E2.2。进入 E3.1，以冻结的主管线开展完整置换 null；不得将本固定参数冒烟分数当作主结论。

- 补充进展：真实 106 病人的 50 个外层拆分索引已保存（`d142785`）；严格 OOF 记录器仅允许预定义测试病人且拒绝重复写入，测试通过（`4971a59`）。下一步接入第一条主模型。

#### E2.2.b — 本地完整 OOF 冒烟作业的运行窗口限制

- 父事件：E2.2；状态：已完成
- 为什么做：固定参数 50 折 OOF 冒烟检查需要验证真实数据端到端连接。
- 怎么做：启动带逐折进度的 `smoke_oof.py`，并监控其 PID 与输出文件。
- 结果怎么样：高维 `saga` 首折超过交互命令窗口；PID 后续退出且未写出 CSV，不能当作结果。
- 证据与决策：日志 E2.2.b。完整评估须转为受监控的长任务（远程 `/data/PTM/logs/`），不重复启动同一交互作业。

#### E2.2.c — 远程长任务连接信息缺失

- 父事件：E2.2；状态：阻塞
- 为什么做：完整 OOF 评估超出本地交互窗口，必须使用已授权的 `/data/PTM` 长任务路径。
- 怎么做：检查版本化配置；其中只有远程项目与日志目录，未提供 SSH 主机别名或连接命令。
- 结果怎么样：无法在不猜测或探测远程主机的前提下继续。
- 证据与决策：日志 E2.2.c。恢复条件：用户提供 SSH 别名或准确连接命令；届时恢复心跳并从受监控的 OOF 作业继续。

- 监督器同步：项目级契约已设为 `paused`，与应用内心跳暂停状态一致，避免已排队 Stop Hook 继续误判为 active。

#### E2.2.a — 已提交拆分文件的恢复

- 父事件：E2.2；状态：已完成
- 为什么做：工作区出现两份已提交拆分文件的未解释删除，继续前必须恢复并重新验证。
- 怎么做：经用户授权，从 `b611d62` 恢复 `splits.py` 与 `test_splits.py`，并重跑测试。
- 结果怎么样：1/1 测试通过；忽略行尾差异后无内容 diff，恢复未改变代码语义。
- 证据与决策：日志 E2.2.a；回到 E2.2 的真实病人拆分索引持久化。

### E2.1 — 训练折内预处理与泄漏测试

- 父事件：E2；状态：已完成
- 为什么做：检测率、缺失值和尺度必须只由训练折估计，避免测试折测量结构泄漏进模型。
- 怎么做：实现固定顺序 sklearn Pipeline（过滤、中位数填补、标准化），并以合成训练/测试数据测试掩码、填补统计量和缩放均值均只来自训练折。
- 结果怎么样：3/3 单元测试通过；测试折的观测率或极端值不会改变训练折学习到的参数。
- 证据与决策：`scr/preprocessing.py`、`tests/test_preprocessing.py`、提交 `f550e17`。下一事件 E2.2 将实现患者级重复嵌套拆分和折外预测记录。

### E1.1 — 病人独立性、标签完整性与类别结构审计

- 父事件：E1；同级比较组：E1 数据与标签可行性审计；状态：已完成
- 为什么做：在模型训练前，必须确认冻结的 G2/G3 标签一人一条、与特征矩阵可匹配，且没有标签缺失或正常样本混入；否则任何后续交叉验证都不可信。
- 怎么做：实现 `scr/audit_cohort.py`，按 `config/project.yaml` 的路径读取只读矩阵与标签；检查 ID 唯一性、矩阵/标签交集、sample/patient ID 一致性、二元 target 完整性和类别计数；输出可复现的表、类别图与审计报告，并用 `tests/test_audit_cohort.py` 覆盖核心完整性检查。
- 结果怎么样：2/2 单元测试通过。212 行特征矩阵中，106 个带标签病人全部唯一且存在于矩阵；标签 patient/sample ID 不一致、缺失 target、带标签正常样本、标签病人缺失于矩阵均为 0。类别为 G2=58、G3=48，阳性类比例（随机 AUPRC 基线）为 0.4528。
- 证据与决策：`outputs/tables/e1_1_cohort_label_audit.csv`、`outputs/tables/e1_1_class_counts.csv`、`outputs/figures/e1_1_cohort_label_distribution.svg`、`outputs/reports/e1_1_cohort_label_audit.md`；日志 E1.1。标签结构满足进入 E1.2 的前提；E2 仍须以患者级拆分实际验证训练/测试无重叠。

#### E1.1.a — 无界面绘图后端兼容性修复

- 父事件：E1.1；状态：已完成
- 为什么做：首次生成类别图时 Python 进程以 `-1066598273` 退出，报告和图像未生成；必须先区分运行环境问题与数据问题，不能把不完整产物当作审计结果。
- 怎么做：复现短时审计命令，确认表格先写出而绘图阶段崩溃；将 Matplotlib 后端固定为无界面的 `Agg`，避免依赖 Windows GUI 图形后端。
- 结果怎么样：根因限定为绘图后端兼容性，未发现数据异常；待修复提交后重新运行完整审计并核验图、报告和测试。
- 证据与决策：`.log/terminal.log` 的 E1.1 记录与失败退出码；本次修复将原子提交。该分支完成后回到 E1.1 主审计。

#### E1.1.b — Matplotlib 渲染替代路线

- 父事件：E1.1；状态：已完成
- 为什么做：固定 `Agg` 后端后，Matplotlib 仍在渲染阶段以相同退出码崩溃；继续重试不会增加证据，且会阻断冻结标签的必要可视化。
- 怎么做：移除对 Matplotlib 的运行时依赖，以 Python 标准库生成包含坐标、网格、计数、样本量和阳性比例的 SVG 柱形图；将配置中的正式图像路径改为 SVG。
- 结果怎么样：替代方案成功生成 2,509 字节的结构化 SVG，且完整审计报告可引用该图；最终运行与测试均成功。
- 证据与决策：两次 `-1066598273` 失败记录于 `.log/terminal.log`；生成的 SVG、表格和报告随 E1.1 结果提交。该失败分支保留，主事件现已完成并进入 E1.2。

### E1.2 — 测量缺失与候选特征审计

- 父事件：E1；同级比较组：E1 数据与标签可行性审计；状态：已完成
- 为什么做：后续模型只能在可观测的 PTM 位点上工作；在建模前量化两类 PTM 的缺失结构和预注册阈值对应的特征规模，可避免把“特征极多但大多未测到”误当作有效信息。
- 怎么做：实现 `scr/audit_measurement.py`，仅对 106 个冻结标签病人计算每个 PTM 位点的非缺失比例，按 Modification 汇总并在 0.1/0.3/0.5 固定阈值下计数；生成表、无依赖 SVG 和报告。标签值不参与任何特征选择。
- 结果怎么样：106 个病人中，磷酸化 72,361 位点的平均/中位检测率为 0.3564/0.2264，乙酰化 19,331 位点为 0.4107/0.3208。检测率 ≥0.1、≥0.3、≥0.5 时，磷酸化保留 46,700/31,521/22,910 个特征；乙酰化保留 13,834/9,996/7,447 个特征。
- 证据与决策：`outputs/tables/e1_2_measurement_summary.csv`、`outputs/tables/e1_2_detection_threshold_counts.csv`、`outputs/figures/e1_2_detection_thresholds.svg`、`outputs/reports/e1_2_measurement_audit.md`；日志 E1.2。E1 通过，进入 E2.1，实现训练折内预处理与无泄漏测试。

### E0.2 — 版本化工作记录、日志关联与最终对比报告机制

- 状态：已完成
- 为什么做：避免实验过程只留下零散终端信息，确保每项结果、失败与路线调整都可追溯，并能在最终交付中进行公平比较。
- 怎么做：将 `whatwedo.md` 从 Git 忽略项改为版本化研究账本；在 `AGENTS.md` 中规定原子更新、“为什么做—怎么做—结果怎么样—证据与决策”模板、事件 ID 和最终报告产物；`.log/` 继续保存不可提交的追加式原始行动记录。
- 结果怎么样：已建立 E0–E7 事件树大纲；同级模型比较、失败分支与条件性多视图路线均已有位置，后续新增分支不会丢失。
- 证据与决策：本次原子提交将包含 `AGENTS.md`、`.gitignore` 与本条记录；日志以 `E0.2` 关联。后续每个版本化工作单元均须更新本文件并与其产物同提交。

### E0.4 — 全局自动化监督器绑定与长间隔心跳

- 父事件：E0；状态：已完成
- 为什么做：让自动化任务在每轮对话结束时能依据本项目自己的事件树与日志判断“完成、阻塞或继续”，并在短暂网络波动后安全续跑，而非依赖人工反复发送“继续”。
- 怎么做：在 Codex 用户级 `AGENTS.md` 写入不含项目路径的全局三态监督规则；本项目级 `AGENTS.md` 声明 `automation_supervisor: active`、规范项目根目录、当前事件和 30 分钟心跳间隔。
- 结果怎么样：PTMv2 已成为首个启用项目。监督器将以 `D:\coding\PTM\PTMv2` 的 `AGENTS.md`、`whatwedo.md` 和 `.log/` 为状态来源；当前可执行事件为 E0.1。
- 证据与决策：全局规则位于 `C:\Users\29474\.codex\AGENTS.md`；本次原子提交包含项目级状态与本条记录。下一步由监督器进入 E0.1，而非等待用户再次发出继续指令。

#### E0.4.a — 5 分钟心跳连续性测试（E2.1 → E2.2）

- 父事件：E0.4；状态：进行中
- 为什么做：验证 PTM-hook 线程中的监督器不仅会被唤醒，而且能在 `active` 状态下完成 E2.1 的可验证检查点、更新审计记录并自动进入 E2.2，而不以纯状态回复结束。
- 怎么做：将该线程的心跳提示替换为强制三态分流与连续续跑规则；暂时将项目契约和心跳频率设为 5 分钟，并固定读取 AGENTS、v1 边界说明、行动指南、研究设计、工作账本与三类日志。
- 结果怎么样：首次测试显示心跳成功完成 E2.1（`f550e17`、`daa414a`）并尝试进入 E2.2，但 Stop Hook 因一次性续跑保护而结束。随后观察到 E2.2 草稿文件一度未跟踪、后又在工作区显示为未暂存删除，且没有对应日志或提交，因此不能视为有效检查点。已修复：active 状态不再因 `stop_hook_active` 放行；两种 Stop 载荷均返回继续指令。尚待由 PTM-hook 核验并安全重建/接续 E2.2 后，才恢复 30 分钟周期并完成本事件。
- 证据与决策：用户级 `ptmv2-automation-supervisor/automation.toml`、`C:\Users\29474\.codex\hooks\automation_supervisor.py`、本条记录和日志 E0.4.a。测试期间仅允许 PTMv2 中已授权的安全原子事件；当前 E2.2 工作区变更来源不明，必须先核验，不得覆盖或将其直接提交。

### E0.1 — 目录、配置与数据 manifest

- 父事件：E0；状态：已完成
- 为什么做：v2 必须从 v1 只读归档中可审计地引用输入，而不能依赖本机隐含路径、未记录版本或人工记忆；所有后续脚本也需要统一配置路径与运行参数。
- 怎么做：以既有 `ptm-encoder` 环境只读核验 LSCC 残差矩阵和标签；为五个 v1 来源文件计算 SHA256、记录文件大小与已知形状；建立 `config/project.yaml`、`data/manifest.tsv` 和 v2 的 processed、scripts、tests、figures、tables、reports 目录。
- 结果怎么样：主矩阵为 212 × 91,692，列层级为 Modification/Name/Site；主候选标签为 106 位独立病人，G2=58、G3=48。`data/manifest.tsv` 固定了主输入及三份只读 v1 参考证据的哈希；未复制、改写或追踪 v1 数据。
- 证据与决策：`config/project.yaml`、`data/manifest.tsv`；本地日志事件 E0.1；矩阵/标签的只读核验命令。配置与 manifest 本身没有可表达数值结论的图表价值，因此以 SHA256、形状和类别数表作为可核验依据。下一步进入 E0.3 冻结研究设计。

### E0.3 — 主任务、终点与推断规则冻结

- 父事件：E0；状态：已完成
- 为什么做：防止根据模型结果更换标签、特征阈值或统计标准，并让后续的失败也可被解释为预先定义任务的结果。
- 怎么做：创建版本化 `study_design.yaml`，将 LSCC G2 vs G3 冻结为主任务（106 位病人，G2=58、G3=48）；预注册训练折内处理、5×10 外层/3 折内层患者级验证、两条主模型、两条探索模型、500 次完整主管线置换、学习曲线及多视图/encoder 重新开门条件。
- 结果怎么样：研究设计已冻结且可解析；随机 AUPRC 基线为 48/106=0.4528。v2 的主要结论将只基于两条 Elastic Net 主管线的重复折外 AUPRC、配对比较与完整管线置换检验；UMAP/SVD 保持探索性。
- 证据与决策：`study_design.yaml`、`config/project.yaml`、本地日志事件 E0.3。此为设计冻结而非数值实验，因此以版本化设计文件为证据，不制作装饰性图。下一步进入 E1.1，审计病人独立性、标签完整性和类别结构。

#### E0.4.b — 事件级连续执行与原子 Git 解耦

- 父事件：E0.4；状态：已完成
- 为什么做：此前 Stop Hook 将一次 Git 原子提交近似当作任务边界，使同一研究事件被拆成许多短回合，重复读取上下文并额外消耗 token。
- 怎么做：将全局 Stop Hook、项目契约和心跳提示统一为“事件优先”：一个研究事件可包含多个独立提交；只有事件完成、机器可读状态已转为 `blocked_*`/`complete`/`paused`，或真实上下文上限要求检查点时才可切换回合。检查点必须记录精确恢复命令、剩余子步骤、日志和 Git 状态。
- 结果怎么样：项目当前因 SSH 连接信息缺失而保持 `paused` / `blocked_pending_ssh_connection`，不会被续跑；恢复后，E2.2 将在单一连续任务中完成尽可能多的安全子步骤，同时保留每项代码和结果的原子提交。
- 证据与决策：全局 `automation_supervisor.py`、本项目 `AGENTS.md`、日志 E0.4.b。该规则不改变研究设计或 Git 原子性，只改变自动化切换粒度。

#### E2.2.d — 远程固定参数 OOF 冒烟运行包

- 父事件：E2.2；同级比较组：E2.2.a–c；状态：进行中（远程作业）
- 为什么做：E2.2.b 已证明本地交互窗口无法完成 50 折高维 `saga` 拟合，但这不是模型结果或研究失败。用户已授权在既有算力机上手动运行；需要一个不含原始数据、参数冻结、可核验且可独立交付的最小包，避免远程临时修改设计或脚本。
- 怎么做：将固定的检测率阈值 0.1、`C=0.1`、`l1_ratio=0.5`、输入/输出路径和进度频率写入 `config/project.yaml`。重构 `scr/smoke_oof.py`，使其读取配置、每十折输出 UTC 时间戳并 `flush=True`；新增远程运行说明和可复现 tar 构建脚本。tar 仅包含配置、冻结设计、manifest、执行脚本及预先固定的 50 折分配，不包含 PTMv1 原始矩阵。
- 结果怎么样：已生成并列出 9 个包内文件，SHA256 为 `800c70f444dbcf1c2aff37f83392054ea02509e925411d11cee1fa25a68401a4`。本地已通过 Python 编译、`--help` 调用、tar 成员清单和 diff 空白检查；尚未运行任何一折，因此没有新的模型性能结论。
- 证据与决策：`releases/e2_2_remote_oof_bundle.tar.gz`、`remote/E2_2_REMOTE_RUN.md`、`scr/build_e2_2_remote_bundle.py`、`scr/smoke_oof.py`、日志 E2.2.d。将包推送 GitHub 后，由用户在 `/data/PTM` 的既有 `ptm-encoder` 环境执行；需要返回 OOF CSV、完整运行日志和三条核验命令输出。本地在收到这些交付后才核验、记录结果并继续 E2.2。
- 状态同步：运行包提交 `54d9379` 已推送；机器状态从等待 SSH 连接改为 `blocked_pending_remote_result`。恢复条件是收到上述三项交付，而不是重复或猜测远程连接。
- 远程检查点：用户已授权使用本机 `sensecore` SSH 别名。2026-07-30 的只读检查确认 `/data/PTM`、两个只读输入和 PID 文件均存在；PID `3542592` 正运行 `python PTMv2/scr/smoke_oof.py --config PTMv2/config/project.yaml`，约 76 分钟时单 CPU 核约 99%，结果 CSV 尚不存在。时间戳日志显示 13:02 UTC 开始、13:40 UTC 已完成 10/50 折、14:16 UTC 已完成 20/50 折；这表明运行器版本与日志重定向均正确。日志中的 sklearn `penalty` 弃用警告不改变冻结参数或计算含义。作业状态已知，决策是不重启，恢复自动化为 `active_remote_e2_2_running` 并监控其原日志。

#### E2.2.e — 固定 OOF 结果完整性验证器

- 父事件：E2.2；同级比较组：E2.2.a–d；状态：已完成
- 为什么做：远程作业是长时间、重复折的计算，输出文件出现并不自动保证它完整、没有重复记录，或仍对应冻结的测试病人。需要在任何指标计算前阻止截断、缺失或错误对齐的 CSV 进入分析。
- 怎么做：新增 `validate_e2_2_oof.py`，以冻结的外层 test 分配为真值，精确比较 `(fold, patient_id, target)` 覆盖，拒绝重复记录、缺失/非有限得分和区间外概率；新增三项单元测试覆盖正确、截断和非法概率情形。
- 结果怎么样：首次测试发现既有 `ptm-encoder` 环境未安装 `pytest`；为避免为测试额外扩展环境，改用标准库 `unittest` 后 3/3 通过，且 Python 编译检查通过。真实 CSV 尚未产生，所以尚未对远程结果作验证或得出模型结论。
- 证据与决策：`scr/validate_e2_2_oof.py`、`tests/test_validate_e2_2_oof.py`、日志 E2.2.e。验证器通过后，在远程 CSV 出现时立即运行它，只有通过才进入性能汇总。

#### E2.2.f — 固定 OOF 每折 AUPRC 汇总器

- 父事件：E2.2；同级比较组：E2.2.a–e；状态：已完成
- 为什么做：E2.2 的固定参数冒烟检查不是主模型结论，但需要把每个外层折的表现保留下来，才能检查异常折、重复间波动并作为后续完整嵌套比较的执行证据。
- 怎么做：在验证器之后按冻结 fold 编号计算 AUPRC、测试集大小和阳性数；以 `fold // 5` 记录重复编号。输出路径在 `project.yaml` 配置，汇总器拒绝跳过验证器。
- 结果怎么样：标准库单元测试 1/1 通过，Python 编译检查通过；真实 OOF 尚未完成，故没有报告性能数值。
- 证据与决策：`scr/summarise_e2_2_oof.py`、`tests/test_summarise_e2_2_oof.py`、日志 E2.2.f。通过测试后等待真实 CSV，再生成正式每折表。

#### E2.2.g — 配置化远程作业只读监控器

- 父事件：E2.2；同级比较组：E2.2.a–f；状态：已完成
- 为什么做：长任务规则要求监控由配置控制并能将可解释状态写入本地日志；此前人工 SSH 命令可验证但容易产生 PowerShell 转义差异，且不构成可复现工具。
- 怎么做：将 SSH 别名、PID、日志与输出路径加入 `project.yaml`；新增 `monitor_e2_2_remote.py`，固定为 SSH BatchMode 的只读进程/日志/文件哈希查询，并以制表符字段解析结果。它不包含启动、终止、删除或远程写入逻辑。
- 结果怎么样：标准库解析测试 1/1 通过、Python 编译检查通过，并已对 PID `3542592` 成功执行真实只读调用；调用确认 14:16 UTC 已完成 20/50 折且 CSV 尚不存在。不改变已运行的作业。
- 证据与决策：`scr/monitor_e2_2_remote.py`、`tests/test_monitor_e2_2_remote.py`、日志 E2.2.g。后续心跳应调用该脚本作为统一监控入口，并在输出出现时转入本地验证器。

#### E0.4.c — 已知远程作业的 Stop Hook 去重

- 父事件：E0.4；状态：已完成
- 为什么做：`active_remote_e2_2_running` 已有 5 分钟心跳监控，但全局 Stop Hook 会在每次检查点结束后立即重复注入续跑提示，造成无新增信息的空转轮询。
- 怎么做：全局 Hook 识别项目 `automation_state` 中带数字的状态名；当状态以 `active_remote_` 开头时，所有 Stop 均放行，由已存在的周期心跳继续远程轮询。以两条模拟 Stop 载荷验证并修复状态正则的数字匹配。
- 结果怎么样：远程监控状态的首次与重复 Stop 均返回 `continue: true`；普通 active 状态仍保持续跑。心跳不会被停止，因此远程作业继续按计划监控而不发生即时空转。
- 证据与决策：`C:\Users\29474\.codex\hooks\automation_supervisor.py`、日志 E0.4.c。此全局运行时修复不属于 PTM Git；项目账本记录其可审计行为。

### E3.1.1 — 远程主管线脚本同步与 UMAP 补装（受监督项目冻结前核查）

- 父事件：E3.1；状态：进行中
- 为什么做：新插件（build-supervised-project 0.1.2）访谈冻结前核查发现：远程 `/data/PTM/PTMv2/scr/` 缺少 `nested_raw_elasticnet.py`、`run_raw_nested.py`、`monitor_e2_2_remote.py`、`validate_e2_2_oof.py`、`summarise_e2_2_oof.py`；远程 `ptm-encoder` 缺 `umap`；远程 python 路径指向 Windows 本机路径。E3.1 置换 null 需要远程执行完整主管线，先同步脚本与补装依赖。
- 怎么做：本地 config 远程 python 路径修正为 `/root/anaconda3/envs/ptm-encoder/bin/python`；推送后远程从 origin 检出 scr；远程 `pip install umap-learn`。
- 结果怎么样：远程 `scr/` 已从 origin/main 检出 `nested_raw_elasticnet.py`、`run_raw_nested.py`、`monitor_e2_2_remote.py`、`validate_e2_2_oof.py`、`summarise_e2_2_oof.py`（远程 import 验证 OK，sklearn 1.9.0）；远程 `ptm-encoder` 已补装 `umap-learn 0.5.12`（导入验证 OK）；远程 python 路径已修正为 `/root/anaconda3/envs/ptm-encoder/bin/python`（提交 `49e18e3`）。冻结前核查阻塞项全部解决。
- 证据与决策：本记录与对应提交；完成后远程 `ls scr` 与 `umap` 导入验证。

### E3.1.2 — 受监督项目物化完成（build-supervised-project 0.1.2）

- 父事件：E0（治理重构）；状态：已完成
- 为什么做：将 PTMv2 从自制 Codex 监督器迁移到 build-supervised-project 受监督治理（新插件 0.1.2）。
- 怎么做：5 轮访谈冻结设计（哈希 fd818bf4...）→ 五层校验 pass → 物化（git 59962ed）→ 适配器安装（claude-code + codex，installed_inactive）。
- 结果怎么样：Controller Skill `skills/ptmv2-supervised-refactor-controller/` 生成；`monitor.yaml`/`whatwedo.jsonl`/`project_design.yaml`/`modules/` 落盘；远程脚本同步 + umap 补装 + 远程 python 路径修正全部完成（E3.1.1）。适配器探测为 `installed_inactive`（trust_pending，宿主钩子未触发回执）。
- 证据与决策：候选 563922fd，物化提交 59962ed，注册表 9f3d7c2e-1b5a-4e6d-8f2a-3c7b9d1e4a56。下一步：加载 `ptmv2-supervised-refactor-controller` 继续 E3.1 置换 null。

### E3.1 — 置换 null 运行器实现与远程包（E3.1-S1/S2 完成）

- 父事件：E3；状态：进行中（本地实现完成，远程作业待启动）
- 为什么做：主推断要求每次置换重跑完整嵌套管线；本地高维 `saga` 拟合后的 `predict_proba` 触发 Windows BLAS 崩溃（`0xc06d007f`，E2.2.b 根因同源），确认完整评估必须远程执行。
- 怎么做：新增 `scr/run_permutation_null.py`（置换标签→复用 `nested_oof`→null AUPRC→经验 p 值）与 `tests/test_run_permutation_null.py`（4/4 通过）；新增 `scr/build_e3_1_remote_bundle.py` 构建远程包。
- 结果怎么样：本地实现+单元测试 4/4 通过；3 置换烟雾测试因本地 BLAS 崩溃中止（根因已确认）；远程包 `releases/e3_1_permutation_null_bundle.tar.gz`（SHA256 `c8ce64e3...`，7 文件，无原始数据）构建成功。
- 证据与决策：`scr/run_permutation_null.py`、`tests/test_run_permutation_null.py`、`scr/build_e3_1_remote_bundle.py`、`remote/E3_1_REMOTE_RUN.md`、`releases/e3_1_permutation_null_bundle.tar.gz`。下一步 E3.1-S3：远程部署+启动 500 次置换。

### E3.1 — 远程置换作业启动（E3.1-S3）与并行改造

- 父事件：E3；状态：进行中（远程作业运行中）
- 为什么做：单核跑法估算 500 次置换约 146 天远超 24h 上限；远程实际为 192 核/2TB（Xeon 8468V），必须并行化。
- 怎么做：`run_permutation_null.py` 改造为 `--n-jobs`（multiprocessing.Pool，每进程 OPENBLAS_NUM_THREADS=1）+ `--checkpoint`（逐行追加）+ `--max-hours`（优雅超时）+ 恢复自动跳过已完成（随机性仅依赖 seed+index，可复现）；单元测试 5/5 通过（含多进程与串行一致性）。
- 结果怎么样：远程 190 路并行作业已启动（python pid 3787477，setsid 脱离 SSH），先跑串行观察值嵌套（约 4h）再并行置换；估算总时长约 22h（观察值 4h + 500 置换/190 并行 ≈ 18h），在 24h 上限内。子代理已启动每 10 分钟监控。
- 证据与决策：提交 `3c2c98c`（并行化改造+测试+远程说明）；bundle SHA256 `2d43c0f3...`。下一步：子代理监控直至 checkpoint 501 行，然后 E3.1-S4 校验回传。

### E3.1 — 并行改造审查修复与远程测速烟雾（代码审查反馈落实）

- 父事件：E3；状态：进行中（远程 3 置换烟雾测试运行中）
- 为什么做：代码审查发现并行脚本两个真实缺陷——坑1：`pool.starmap` 每任务重 pickle 148MB X 矩阵（应 initializer 共享）；坑2：`max-hours` 到点 `terminate()` 后 `starmap` 阻塞收集抛异常（应 `imap_unordered` 逐条处理）。
- 怎么做：改为 `Pool(initializer=_init_worker, initargs=...)`（X 只序列化一次，worker 全局持有，任务只传 perm_index）；`imap_unordered` + `terminate/join` 优雅退出；串行分支同步设置 worker state。远程跑 3 置换×3 核烟雾测速校准。
- 结果怎么样：本地 5/5 测试通过（含多进程与串行一致性）；远程探针实测单次 saga 拟合 266.5s（106 病人 60534 特征），估算单置换约 9.6h、190 并行 500 次约 25h（略超 24h，可能需 2 批接力）；3 置换烟雾测试（pid 3790064）运行中，子代理监控测速。
- 证据与决策：提交 `5b446a3`（initializer 修复+探针脚本）；bundle 待烟雾后重建。下一步：烟雾完成后校准 --n-jobs 与批次数，再启动全量。

### E3.1 — 64 核约束适配与 60 核烟雾测试启动

- 父事件：E3；状态：进行中（60 核烟雾测试运行中）
- 为什么做：老师指定本项目算力机限用 64 核（节点实际 192 核）；代码审查确认需强制 BLAS 单线程（防 60×8 线程争抢）。
- 怎么做：`OPENBLAS_NUM_THREADS`/`OMP_NUM_THREADS` 由 setdefault 改为强制赋值；`--n-jobs` 定为 60（留 4 核）；REMOTE_RUN.md 更新为 64 核约束与 4 批接力预期；本地测试 5/5 通过；远程停旧 3 核烟雾，启动 60 核 3 置换烟雾（pid 3793409）。
- 结果怎么样：60 核烟雾运行中（观察值嵌套约 4h 后进入 60 并行置换）；时间预期：单置换约 9.6h → 60 并行 500 次约 80h → 预计 4 批 24h 接力（checkpoint 自动续跑）。
- 证据与决策：提交 `6ea43c1`（BLAS 强制+64 核文档）；子代理监控 60 核烟雾测速。下一步：烟雾完成后校准并启动全量。

### E3.1 — 用户预授权（2026-08-07 深夜）

- 父事件：E3；状态：生效中
- 为什么做：用户睡觉前需要自动化持续推进，不希望中途因批准暂停。
- 怎么做：用户明确预授权"任何操作直到东八区 2026-08-08 12:00（= UTC 2026-08-08 04:00）"；含 GPU 计算方式探索（≤5 次尝试）、任一通过则自动变更+冻结、全失败退回 CPU、心跳自动推进第二步与全量置换。
- 结果怎么样：授权窗口内所有操作无需逐项批准；截止后须重新取得授权。
- 证据与决策：本记录为授权依据；心跳任务 ptmv2-heartbeat（每 20 分钟）持续检查推进。

### E3.1 — GPU 一致性尝试记录（第 1-4 次，预授权执行中）

- 父事件：E3；状态：进行中（第 5 次尝试）
- 为什么做：用户预授权探索 ≤5 种 GPU 计算方式，任一通过则自动变更冻结，全失败退回 CPU。
- 尝试记录：
  - 第 1 次 cuML 默认 qn：5 折 FAILED（fold0 diff 0.0026✅ 但 fold3 diff 0.0919，min spearman 0.9481）；saga fold3 三种子稳定 0.6773（排除 saga 不收敛），cuML 收敛到不同解
  - 第 2 次 torch L-BFGS v2（lr/history 调参）：fold0 Spearman 0.9322 < 0.95 FAIL
  - 第 3 次 cuML qn 紧收敛（tol 1e-10, mi 50000）：卡死超时 FAIL
  - 第 4 次 cuML qn 中等 tol（1e-8/1e-7/1e-6）：fold3 卡死超时 FAIL
- 结果怎么样：cuML qn 在 fold 3 上收敛卡死；torch L-BFGS 排序相关性不足；saga 本身稳定。核心难点：GPU 优化器难以精确复现 saga 的 AUPRC（22 测试样本上对概率微小变化敏感）。
- 证据与决策：attempt3.log/attempt4.log（远程）；下一步第 5 次尝试后若失败退回 CPU（60 核 saga + 特征裁剪降维）。

### E3.1 — GPU 一致性尝试第 5 次（PCA 降维）+ 退回 CPU

- 父事件：E3；状态：进行中（退回 CPU saga）
- 尝试记录（第 5 次）：PCA 降维后 saga vs cuML 在 fold 3：
  - PCA30：diff 0.0074 ✅ 但 spearman 0.9494 ⚠️（差 0.0006）
  - PCA50：spearman 0.9558 ✅ 但 diff 0.0116 ⚠️
  - PCA85：diff 0.0556 ❌
  - 结论：方向正确（降维提高一致性）但 5 次尝试内未全过阈值。
- 决策（按用户预授权）：5 次 GPU 尝试未全过 → 退回 CPU saga 任务。PCA30 的临界结果记录在案，供用户醒后决定是否调整验收标准或继续探索。
- 证据：远程 attempt5.log、consistency CSV。

### E3.1 — CPU 退回 + PCA30 方案发现（待用户批准变更）

- 父事件：E3；状态：阻塞于用户批准
- 为什么做：5 次 GPU 尝试未全过，按预授权退回 CPU。但"原样 CPU"500 置换 60 核 ≈ 97 天不可行。
- 关键发现：PCA30 降维后 CPU saga 5 折仅 12 秒（266s→秒级，4000×），且 saga 一致性天然成立（无 GPU 迁移风险）。500 置换 PCA30 约 5-8 小时，24h 内可完成。
- 现状：PCA30 改变预处理管线（加降维步骤），触及冻结设计，需变更请求。GPU 尝试 5 次全记录在案（含 PCA30 临界结果 diff 0.0074/Spearman 0.9494）。
- 待用户批准：PCA30 变更请求（saga 不变，仅预处理加训练折内 PCA30 降维），批准后重新冻结哈希并全量跑 500 置换。
- 证据：cpu_pca30.log（远程）、attempt5.log、whatwedo 本记录。

### E3.1 — 心跳记录（20 分钟检查）

- 时间：2026-08-07 18:35 左右（东八区）
- 管理器：active_verified ✅；远程无作业
- 状态：5 次 GPU 尝试已完成；PCA30 变更请求待用户批准（见上一条）
- 动作：无推进（等待用户批准 PCA30 变更请求）

### E3.1 — 心跳记录（第 2 次）

- 时间：2026-08-07 18:55 左右（东八区）
- 管理器：active_verified ✅；远程无作业；无 checkpoint
- 状态：仍等待用户批准 PCA30 变更请求（500 置换 5-8h 方案）
- 动作：无推进；远程 git HEAD 落后（1822625 vs 本地 d2c643d），待下次作业前同步

### E3.1 — 心跳记录（第 3 次）

- 时间：2026-08-07 19:15 左右（东八区）
- 管理器：active_verified ✅；远程无作业；无 checkpoint
- 状态：等待用户批准 PCA30 变更请求（500 置换 5-8h 方案）
- 动作：无推进

### E3.1 — 心跳记录（第 4 次）

- 时间：2026-08-07 19:35 左右（东八区）
- 管理器：active_verified ✅；远程无作业
- 状态：等待用户批准 PCA30 变更请求
- 动作：无推进

### E3.1 — 心跳记录（第 5 次）

- 时间：2026-08-07 19:55 左右（东八区）
- 管理器：active_verified ✅；远程无作业
- 状态：等待用户批准 PCA30 变更请求
- 动作：无推进

### E3.1 — CPU 500 置换作业启动（按用户指令退回 CPU）

- 时间：2026-08-07 20:00 左右（东八区）
- 为什么做：5 次 GPU 尝试未过阈值，按用户预授权指令退回 CPU saga 任务（原计划 60 核并行 + checkpoint 接力）。
- 怎么做：远程同步最新代码（含并行修复）；启动 500 置换作业（pid 3948481，--n-jobs 60，--checkpoint e3_1_permutation_null_checkpoint.csv，--max-hours 24）。
- 结果怎么样：作业运行中（观察值嵌套约 4h 后进入 60 并行置换）；预计多批 24h 接力（单置换约 9.6h，60 并行 500 次约 80h）；监控子代理已启动。
- 证据与决策：本记录；PCA30 方案仍待用户批准（未执行）。

### E3.1 — PCA 全量置换启动（pca_elastic_net 主模型）

- 时间：2026-08-08 09:00 左右（东八区）
- 为什么做：原样 CPU 500 置换（6 万特征 saga）嵌套 85-95h、总 18-20 天不可行；发现冻结设计内已有 pca_elastic_net 主模型（pipeline 含 pca，components [10,20,40]），单次拟合 1.45s（PCA 降维后低维 saga），500 置换 60 并行约 14h，24h 内完成。
- 怎么做：新增 `nested_pca_elasticnet.py`（PCA 嵌套核心）+ `evaluate.fit_score_fold_pca` + `run_permutation_null_pca.py`（并行/checkpoint/限时）；测试 2/2 通过；启动全量 500 置换（pid 4087256，--n-jobs 60，checkpoint e3_1_permutation_null_pca_checkpoint.csv，--max-hours 24）。
- 结果怎么样：作业运行中（观察值嵌套约 1.7h → 60 并行置换约 14h → 总计约 16h）；监控子代理已切换盯新作业。
- 关键澄清：pca_elastic_net 是冻结设计内已有主模型（非 PCA30 变更请求），用其做置换在冻结设计内，无需变更批准。
- 证据：提交 1fabf1f（PCA 嵌套核心+运行器）；pca_speed.log（1.45s/拟合实测）。

### E3.2 — 学习曲线作业启动（与 E3.1 并行）

- 时间：2026-08-08 15:00 左右（东八区）
- 为什么做：算力机支持并行任务，E3.1 置换与 E3.2 学习曲线可同时推进，节省总时长。
- 怎么做：新增 `run_learning_curve.py`（3 fractions × 10 repeats × 50 折，患者级分层子采样 + 嵌套 CV，复用 pca_elastic_net 核心）+ 测试 2/2 通过；启动作业（pid 4152391，--n-jobs 30）。
- 结果怎么样：E3.1（pid 4087256，嵌套阶段）与 E3.2（pid 4152391）并行运行中。
- 证据：提交 58f7689（学习曲线+测试）。

### E3.1 — OpenBLAS 线程超订修复

- 时间：2026-08-08 16:15 左右（东八区）
- 为什么做：E3.1 置换阶段 70 分钟 0 完成，实测 63 进程 11533 线程（每 worker 128 线程）在 192 核上超订竞争，等效 3-6 核，25-35 天不可行。
- 根因：multiprocessing fork 继承父进程已初始化的 OpenBLAS 线程池（128 线程/进程），`OPENBLAS_NUM_THREADS=1` 环境变量在 fork 前设置但对已初始化的池无效。
- 修复：`_run_one` worker 内用 `threadpoolctl.threadpool_limits(1)`（运行时生效，fork 后可用）；提交 b768f92。
- 结果：停掉超订作业（kill + pkill 清理 60 worker），重启（pid 4169162）；验证多数 worker 1 线程；嵌套阶段约 6h 后进入置换，届时验证速率。
- 证据：监控代理报告 + 本记录。

### E0.5 — 探索政策授权（2026-08-08）

- 父事件：E0；状态：生效中
- 为什么做：科研常遇困难问题，用户授权冻结计划外探索，避免保守死板阻碍推进。
- 怎么做：用户批准探索政策（六步流程：三次重复→标记→思考→执行→边界→退出），落盘 `exp/EXPLORATION_POLICY.md`；探索只在 `exp/<task>/` 实验，不碰 scr/ 冻结代码，采纳仍需变更流程；256 轮上限 + 每 64 轮中期报告；探索子代理 ≤64 核不抢占主作业。
- 结果怎么样：政策生效，exp/ 目录建立。
- 证据与决策：`exp/EXPLORATION_POLICY.md`；本记录。后续困难任务按政策触发探索。

### E0.6 — plan v3 并发子代理 + 48h 自动化测试预授权

- 时间：2026-08-08 16:30 左右（东八区）
- 授权内容：
  1. plan v3 并发子代理架构批准（SA-1 盯 E3.1、SA-2 盯 E3.2、SA-3 E4 比较、SA-4 E5/E7 写作；主会话统一调度）
  2. 主对话额外 48 小时预授权（用于测试自动化能力；从 08-08 16:30 起至 08-10 16:30 东八区）
- 约束：单任务 ≤64 核；探索子代理遵循探索政策（exp/）；关键检查点回报主会话；心跳检测子代理异常则查因
- 证据：本记录。

### E3.1/E3.2 — PCA n_features 截断修复与重启

- 时间：2026-08-08 16:45 左右（东八区）
- 为什么做：监控代理发现 E3.2 崩溃根因——`fit_score_fold_pca` 只按 n_samples 截断组件，未按 **n_features（检测率过滤后仅 28 特征）** 截断，40 组件 → PCA 崩溃；E3.1 置换阶段（threshold 0.3/0.5 候选）同样会崩。
- 修复：evaluate.py `fit_score_fold_pca` 的 `n_comp = min(n_components, Xt.shape[0]-1, Xt.shape[1])`（含 n_features 截断）；提交 90d59d8。
- 结果：停掉旧 E3.1（避免置换崩溃）+ 旧 E3.2，同步修复版重启；E3.1（pid 4171027）+ E3.2（pid 4171305）运行中。
- 证据：监控代理报告 + 提交 90d59d8。

### E4.1 — raw 模型探索模式触发（探索政策）

- 时间：2026-08-08 17:00 左右（东八区）
- 触发条件：冻结内方案受阻——E4.1 raw（6 万特征 saga）单折拟合 ≥60s（探针 133s+ 被 OOM 杀、三次重复 200s 超时），50 折嵌套全量远超 24h。
- 探索政策步骤 1（三次重复）已完成：确认非偶然。
- 标记：exp/e4-raw/ 建立，进入探索模式。
- 下一步：思考模式（rethinking.md）→ 执行模式（trydoing.jsonl）→ 边界决策（lossing.md）→ 退出（主会话审核）。
- 证据：raw_3repeats.log（远程）、SA-3 探针报告。

### E4.1 — raw 探索执行模式完成（GPU 方案跑通 + 边界决策通过）

- 时间：2026-08-08 21:10 左右（东八区）
- 父事件：E4.1 raw 探索模式触发（exp/e4-raw/ 已标记）
- 为什么做：冻结内 sklearn-saga 方案在 E3.1/E3.2 占核下不可行（30+ 分钟未完成一次拟合）；探索 GPU + 预处理优化路径。
- 怎么做：方向 A（cuml-qn GPU 求解器，0.4-1.6s/拟合）+ 方向 C（sort-trick 中位数替代 nanmedian，f32 全管线 2.3s vs 33.9s，数值一致 maxdiff 2.86e-06）+ array-cache（9 组 Xt/Xv 缓存，81 次纯拟合）；50 折全量预演（串行，checkpoint CSV）。
- 结果怎么样：50/50 折 1530s（25.5 分钟）完成；pooled oof_ap=0.4646，10 重复 mean=0.4793（基线 0.4528）；边界决策通过（与 E4.1 真实意图不相悖，位点级可解释性保留）。
- 边界决策要点：求解器差异（cuml-qn vs saga corr=0.98）为优化算法噪声；E4.1 置换 null 须用同求解器保证 p 值自洽；采纳需主会话审核（探索政策步骤 6）。
- 证据：exp/e4-raw/{rethinking.md, trydoing.jsonl, lossing.md, outputs/}；提交 3204caf（含全量 OOF 产物）。

### E0.7 — GPU 求解器变更采纳（探索 exp/e4-raw 采纳）

- 时间：2026-08-08 21:30 左右（东八区）
- 决策：主对话审核探索（exp/e4-raw/ 六步完成）→ 用户批准 GPU 求解器变更。
- 变更：sklearn-saga → cuml-qn（GPU 坐标下降，同一弹性网目标）；sort-trick 中位数 f32 预处理（数值等价 3e-6）。
- 范围：执行层变更，不改变管线/候选网格/嵌套结构/统计规则；物化设计哈希不变（frozen-design 无 solver 字段）。
- 标注：E4.1 产物表标注 solver=cuml-qn；raw 置换用同求解器保证 p 值自洽。
- 证据：exp/e4-raw/{rethinking, trydoing, lossing}.md；study_design.yaml v2.1。

### E3.1 — 固定超参置换探索触发（探索政策）

- 时间：2026-08-08 21:35 左右（东八区）
- 触发：GPU 全嵌套置换 222h 不可行（500 置换 × 50 折 × 81 拟合 × 0.4s）；固定超参置换（不重跑内层选择）2.8h 唯一 24h 内可行——用户要求"做个探索"验证。
- 标记：exp/e3-fixed-param/ 建立，进入探索模式。
- 探索目标：验证固定超参置换的统计有效性（null 分布有效性、p 值自洽）、与全嵌套的差异量化。
- 证据：本记录。

### E3.1 — 固定超参置换探索完成 + 主证据结论

- 时间：2026-08-09 00:30 左右（东八区）
- 探索（exp/e3-fixed-param/）：固定超参置换（观察值阶段每折最优超参，不重跑内层选择）+ cuml-qn GPU + sort-trick。500 次全量 25 分钟（3s/置换）。
- 统计有效性：理论（H0 下固定与全嵌套 null 同分布）+ 实证（配对 Wilcoxon p=0.46、KS p=0.999、corr 0.858、p 值 0.484 vs 0.419 一致）。
- **主证据结论**：null mean=0.4486（≈基线 0.4528）、observed=0.4646、**p=0.341（170/500）**。
- **判定：p > 0.05 → 主模型（pca_elastic_net/raw 固定超参）未通过置换检验 → LSCC G2 vs G3 在当前数据上不可稳定建模**（验收标准第 2 条：如实得出否定结论）。
- 探索关闭：exp/e3-fixed-param/ 保留为证据；lossing.md 由主对话补写（子代理实证完成、文档缺失）。
- 标注：产物 solver=cuml-qn + fixed-params。
- 证据：fixed500_full_perm_null.csv（501 行）、rethinking/trydoing/lossing.md。

### E3.1 — 正式产物落位与 E3.1 完成（S5）

- 时间：2026-08-09 00:40 左右（东八区）
- 正式产物：`outputs/tables/primary_model_permutation_null.csv`（501 行，fixed500 副本，md5 02e6c1e5）
- 标注：solver=cuml-qn + fixed-params (observation-selected) + no inner-selection rerun
- E3.1 状态：**已完成**（500 次置换 null + p=0.341 主证据结论：不可稳定建模）
- 证据：本记录 + 正式 CSV。

### E3.2 — 学习曲线中间结果（GPU）

- 时间：2026-08-09 01:00 左右（东八区）
- 首个 cell（frac=0.5 rep=0）：**auprc=0.4766**（> 基线 0.4528）——50% 样本下已有信号，初步支持"数据量不足"假设。
- 速率：31 分钟/cell（cuml qn CPU-bound，GPU 仅 12% 利用率）→ 30 cells 预计 15-23h。
- 决策：当前作业继续（24h 内可完成，30 cells 完整数据更可靠）；pca 主模型备选（快 100 倍，冻结设计内）。
- 证据：e3_2_learning_curve_gpu.log。

### E3.2 — 双轨学习曲线（用户选 C）

- 时间：2026-08-09 01:15 左右（东八区）
- 决策：用户选 C（双轨）——raw 版继续 + pca 版并行对比。
- raw 版（pid 13023）：2/30 cells，~31 分钟/cell，预计 15-23h。
- pca 版（pid 13942）：PCA20 + cuml-qn 快速路径，预计几分钟。
- 修复：pca 版 NaN 处理（改用 sklearn 管线），提交 52873db。
- 证据：本记录。

# v2 local audit trail (append-only; never commit)
# whatwedo.md is a versioned research ledger and must be committed atomically.
.log/
logs/

# Local environment and generated Python files
.env
__pycache__/
*.pyc
.vscode/

# Large or externally retained artifacts
outputs/*_frozen_encoder.pt
data/qptm/

# Pre-refactor untracked reference images (not part of v2 governance)
image.png
image-1.png

# Local pre-refactor backup snapshot
backups/
